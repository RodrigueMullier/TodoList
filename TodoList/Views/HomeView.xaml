<Page x:Class="TodoList.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TodoList.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="#55BBFF"
      Title="HomeView">

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="To-Do List" FontSize="48" Foreground="#222222" HorizontalAlignment="Center"/>
            <Label Content="de Rodrigue MULLIER" FontSize="20" HorizontalAlignment="Center" Foreground="#222222" Margin="0,0,0,50" />
            <Button Content="Accéder aux tâches" 
                Background="#0055FF" 
                Foreground="White" 
                Height="32" Width="250" 
                BorderBrush="#0022FF" 
                Command="{Binding NavigateToTasksCommand}">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
</Page>
