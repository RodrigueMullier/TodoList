<local:BasePage x:Class="TodoList.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TodoList.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="{StaticResource BackgroundColor}"
      Title="Accueil">

    <StackPanel VerticalAlignment="Center">
        <Label Content="To-Do List" Style="{StaticResource TitleLabel}" />
        <Label Content="de Rodrigue MULLIER" Style="{StaticResource SubTitleLabel}" HorizontalContentAlignment="Center" />
        <Separator Height="32" Background="Transparent" />
        <Button 
                Content="Accéder aux tâches" 
                Width="250" 
                Style="{StaticResource DefaultButton}"
                Command="{Binding NavigateToTasksCommand}" />
    </StackPanel>
</local:BasePage>